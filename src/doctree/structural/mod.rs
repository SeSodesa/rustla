/// This module contains sructural level node specifications.

use super::*;

#[derive(Debug)]
pub struct Section {
  id: usize,
}

#[derive(Debug)]
pub struct Topic {
  id: usize,
}

#[derive(Debug)]
pub struct Transition {
  id: usize,
}
